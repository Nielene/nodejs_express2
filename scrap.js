const ai = () => {
  let comp = Math.random();

  switch (comp) {
    case comp >= 0.0 && comp <= 0.33: r
      break;
    case comp >= 0.34 && comp <= 0.66: p
      break;
    case comp >= 0.67 && comp <= 1: s
      break;
    default:
      null;
  }
}
